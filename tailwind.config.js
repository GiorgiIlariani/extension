/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./src/**/*.{html,js}"], // Paths to scan for Tailwind classes
  theme: {
    extend: {}, // Extend default theme if needed
  },
  plugins: [], // Add Tailwind plugins here
};
